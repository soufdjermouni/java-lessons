package com.lessons.polymorphism.Interfaceoverriding;

public interface Drawable {
    //This interface has 2 methods, printAreaFormula() and printName(), which are effectively
    // abstract like playSound() in the previous example
    void printAreaFormula();
    void printName();
}
